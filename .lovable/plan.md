

# ✅ COMPLETED: Treatment Not Diagnosis + Brain Integration

Both fixes deployed:

1. **`generate-90day-strategy`** — All 3 layers (Goals, Milestones, Daily Actions) now enforce "Treatment Not Diagnosis" rules with banned verbs (identify, check, test, evaluate) and required verbs (perform, execute, practice, drill). Pillar-specific examples injected (Image → mewing/face yoga, Consciousness → shadow work rituals).

2. **`generate-phase-actions`** — Now imports `buildContext` from shared contextBuilder. Has full access to biological profile (diet, substances, sleep), willingness boundaries, and user identity. Same Treatment-Only rules enforced. Generates `execution_template` and `action_type` per mini-milestone.

**To see the fix**: Force regenerate the 100-day plan. Existing missions in the database will NOT change automatically.
