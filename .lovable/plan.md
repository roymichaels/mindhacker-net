# Fix bleed-through: opaque backgrounds for modals, pages, and hubs\n\n## Problem\n\nLarge surfaces (sheets, modals, hub pages) currently use semi-transparent backgrounds like `bg-card/95`, `bg-background/85`, `bg-background/80` etc. On top of an active chat or another page they bleed through (visible in the Free Market screenshot — chat text is readable behind the modal).\n\n## Fix scope\n\nTwo-tier rule going forward:\n\n1. **Page-level / modal-level containers (must be fully opaque):** route pages, full-screen overlays, sheets/drawers/dialogs, hub shells, settings modal, subscription modal, brain view, mind-os sheet, composer-actions sheet, voice mode, OS drawer, hypnosis layer surface.\n2. **In-content chips / inline cards (stay translucent):** ambient cards, artifact pills, floating buttons, dock chips, small toolbar chips inside `InteractiveAION`. Untouched — they're meant to feel glassy on top of *their own* page background.\n\n## Changes\n\n### A. Shared shadcn primitives — opaque content + stronger overlay\n\n- `src/components/ui/sheet.tsx` — `SheetContent`: `bg-white dark:bg-background` → `bg-background` (drop `dark:`), keep `dark` variant via tokens. Overlay: `bg-black/80` → `bg-black/90`.\n- `src/components/ui/dialog.tsx` — `DialogContent` outer wrapper: replace `bg-white dark:bg-gradient-to-br dark:from-gray-950 dark:via-gray-900 dark:to-gray-950` with solid `bg-background` (or `bg-card`). Overlay `bg-black/80` → `bg-black/90`.\n- `src/components/ui/drawer.tsx` — `DrawerOverlay` `bg-black/80` → `bg-black/90`. `DrawerContent` already `bg-background` (good).\n\n### B. Project-level sheets & modals — drop the `/XX` on the outer surface\n\n- `src/components/shell/MindOSSheet.tsx` line 133: `bg-card/95` → `bg-card`.\n- `src/components/aurora/composer/ComposerActions.tsx` line 71: `bg-card/95` → `bg-card`.\n- `src/components/shell/OSDrawer.tsx` line 67: `bg-card/95` → `bg-card`.\n- `src/components/aion/InteractiveAIONHost.tsx`: backdrop `bg-black/40` → `bg-black/85`, suspense fallback `bg-background/95` → `bg-background`. The slide-in `<aside>` already uses solid `bg-background` — keep.\n- `src/components/aion/ChatHistorySheet.tsx` — outer sheet body opaque (audit and remove `/XX` on the panel container; keep individual message bubbles `bg-card/60`).\n- `src/components/subscription/SubscriptionsModal.tsx` — outer container `bg-...XX` → solid.\n- `src/components/settings/SettingsModal.tsx` and tab files (`ProfileSettingsTab`, `AuroraPreferencesTab`, `AppearanceSettingsTab`, `AccountSettingsTab`) — outer page/modal wrappers opaque; inner cards may keep current opacity.\n- `src/components/aurora/AuroraVoiceMode.tsx` line 44: `bg-background/95` → `bg-background`.\n- `src/features/brain/BrainView.tsx` and `BrainGraphCanvas.tsx` — outer canvas surface opaque.\n- `src/components/aion/layers/HypnosisLayer.tsx` line 59: keep (intentional dim of underlying scene).\n\n### C. Hub & route pages — opaque page backgrounds\n\nEach of these renders as a full route. Their root wrapper must be solid `bg-background`:\n\n- `src/pages/AuroraPage.tsx`\n- `src/pages/Business.tsx`\n- `src/pages/ArenaHub.tsx`\n- `src/pages/CoachHub.tsx`\n- `src/pages/CreatorHub.tsx`\n- `src/pages/Courses.tsx`\n- `src/pages/CourseWatch.tsx`\n- `src/pages/CommunityThread.tsx`\n- `src/hallway/HallwayShell.tsx`, `src/hallway/RoomEnvironment.tsx`, `src/hallway/surfaces/BodyHypnosisSurface.tsx`\n- `src/presence/PresenceShell.tsx`\n\nFor each: locate the outermost wrapper and remove any `bg-background/XX` / `bg-card/XX` on that node; replace with solid `bg-background`. Keep inner cards/chips untouched.\n\n### D. Composer dock surfaces (top-of-keyboard, must not show chat behind)\n\n- `src/components/aurora/AuroraLayout.tsx` line 43: `bg-background/85` → `bg-background`.\n- `src/components/aurora/AuroraChatInput.tsx` line 132: `bg-background/95 supports-[backdrop-filter]:bg-background/80` → `bg-background`.\n- `src/presence/ArtifactsDock.tsx` — outer dock surface opaque.\n\n### E. Header\n\n- `src/components/Header.tsx` — header wrapper `bg-background/XX` → `bg-background` (it's pinned over content). Audit; only flip page-level header bar, not internal pill chips.\n\n## Out of scope (intentionally left translucent)\n\nThese are *inside-page* glass elements — they have their own page bg behind them, so they don't bleed across surfaces:\n\n- `AmbientContextCard`, `ArtifactLayer` artifact pills, `AuroraActionConfirmation`, the small chips inside `InteractiveAION` (`bg-card/40`), `VoiceNoteButton`, `PullToRefreshIndicator`, `BugReportWidget`, `ProjectCard`, `OrbNFTCard`, diagnostics sheet internals.\n\n## Acceptance\n\n- Opening any sheet/dialog/drawer/modal on top of `/aurora` (chat) or any hub fully obscures the surface behind it — no readable text, no visible orb.\n- Hub pages (Free Market, Business, Arena, Coach, Creat