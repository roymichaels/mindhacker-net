\n# Flatten MindOS hub + chat dock cleanup\n\nTwo changes shipped together:\n\nA. **Remove the standalone MindOS hub layer.** The sidebar opens environments directly (`/strategy`, `/hypnosis`, `/fm`, `/community`, `/learn`). No more \