# Plan\n\nThe reason it still isn’t the homepage is simple:\n- `/` still renders the old `Index` marketing page in `src/App.tsx`\n- the orb click opens `AIONChatPanel`, but the main MindOS chat route renders a different surface (`AIONPage` via `src/pages/MindOS/ChatPage.tsx`)\n- so the app currently has two separate AION chat experiences, and the actual root route is still not wired to the orb chat\n\n## What I’ll change\n\n1. **Make the authenticated home route open AION first**\n- change the root app flow so signed-in users land on the AION chat as the main app screen\n- add a safe redirect for `/index` to the same destination if needed\n- keep unauthenticated users on the public landing flow unless you want that removed too\n\n2. **Use the orb chat as the real main surface**\n- stop treating the orb-opened chat and the page chat as two different UIs\n- extract the shared AION chat body from `AIONChatPanel`/`AIONPage` into one reusable component\n- render that same experience both in the fullscreen route and in the orb modal, so the homepage matches exactly what opens from the orb\n\n3. **Prevent duplicate AION UI**\n- hide or disable the floating orb launcher while already on the AION home screen\n- keep the orb available on other tabs/modal hubs\n\n4. **Validate the route behavior**\n- confirm `/`, `/index`, and `/mindos/chat` all land on the AION home experience\n- confirm the header + bottom tabs remain visible and the chat sits between them as requested\n\n## Technical notes\n- Main issue is in `src/App.tsx` where `path=\