# Fix: AION chat is not responding\n\n## Root cause (confirmed)\n\n`src/hooks/aurora/useAuroraChat.tsx` posts the user message to:\n\n```\nconst MINDOS_CHAT_URL = `${import.meta.env.VITE_AGENT_API_BASE_URL || ''}/api/mindos-chat`;\n```\n\n`VITE_AGENT_API_BASE_URL` is not set in this Lovable project (no Node \