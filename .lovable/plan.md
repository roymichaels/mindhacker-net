# MindOS Shell Rebuild — Audit + Plan (no code yet)\n\n## 1. UX diagnosis (what's wrong right now)\n\nThe shell has the *new* OS pieces (`OSDrawer`, `MindOSSheet`, `AIONPresenceButton`) but they coexist with the *old* shell. Result: stacked layers, web feel, no dominance.\n\nConcretely, in `DashboardLayout` and around it:\n\n- **Three navigation systems running at once**\n  - `OSDrawer` (new left sheet) ✓\n  - `BottomTabBar` / `BottomHudBar` / `DesktopSideNav` (legacy) — still mounted by other surfaces\n  - `AuroraDock` rendered as a *full-screen* `inset-0 z-[9999]` overlay (it's not a dock anymore — it eats the whole screen and competes with the shell header)\n- **Two orbs**: `AIONPresenceButton` in the header AND `StandaloneMorphOrb` inside `AuroraDock` top bar.\n- **Header still has theme color logic, ambient glows, and `ChromeGate(aurora-dock)`** — that's why it feels \