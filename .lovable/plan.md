## Back Button Consolidation Plan\n\nGoal: a single, predictable back affordance everywhere it's needed — modals, secondary pages, and inner hubs — replacing the floating chevron with a header-integrated button.\n\n### Problem Summary\n\n- `FloatingBackButton` lives detached from any header, sometimes hidden under chrome on shorter viewports.\n- Routes that hide it (`/`, `/aurora`, `/brain`, `/outer-world`) are also primary tabs — fine.\n- `ProfilePage` (the screenshot) is a portal modal opened over `/index`. The floating chevron is suppressed because the URL is `/`, so users only get an `X` close — no way to \