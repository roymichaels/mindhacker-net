/**
 * FMHudSidebar — Left sidebar for FM (Free Market) hub.
 * DEPRECATED: No longer used. FM uses right sidebar only.
 * Kept as empty export to prevent import errors during cleanup.
 */
export function FMHudSidebar() {
  return null;
}
