import type { ArtifactComponentProps } from '@/lib/aion/artifactRegistry';
// Results vary per pillar; rendered inline by SummonRoute fallback already.
export default function PillarResultsArtifact(_p: ArtifactComponentProps) { return null; }
