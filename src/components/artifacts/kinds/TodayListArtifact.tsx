import type { ArtifactComponentProps } from '@/lib/aion/artifactRegistry';

export default function TodayListArtifact(_props: ArtifactComponentProps) {
  return (
    <div className="p-6 text-sm text-muted-foreground">
      Today list artifact — coming online soon.
    </div>
  );
}