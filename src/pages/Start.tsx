/**
 * Start — Unified Identity Activation entry point
 * Route: /start
 */
import { ActivationFlow } from '@/components/activation/ActivationFlow';

const Start = () => {
  return <ActivationFlow />;
};

export default Start;
