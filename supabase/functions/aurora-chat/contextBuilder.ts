/**
 * Layer 1: Context Builder (deterministic)
 * Re-exports from shared module for local imports within aurora-chat.
 */

export { buildContext, type AuroraContext } from "../_shared/contextBuilder.ts";
